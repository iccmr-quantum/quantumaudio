import setuptools

setuptools.setup(name='quantumaudio', version='0.1.2', description = 'A python module for building circuits for quantum audio encoding/decoding.', author='Itaborala', install_requires=['qiskit', 'numpy', 'ipython', 'matplotlib', 'bitstring'], author_email='', packages=['quantumaudio'], zip_safe=False)
